<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yakx.web.mapper.ClassroomReserveMapper">

    <sql id="column">
        cs.id, cs.room_id, cs.is_ezviz, cs.class_number, classroom.room_name,classroom.room_address, classroom.is_ezviz, classroom.device_serial,
        cs.course_id, course.course_name, course.course_color,
        cs.teacher_id, teacher.teacher_name,
        cs.class_id, clsases.class_name,
        cs.date_time, cs.begin_time, cs.end_time,
        cs.reason, cs.remark, cs.sequence_total, cs.password,
        cs.create_by, cs.create_time, cs.update_by, cs.update_time
    </sql>

    <select id="getList" resultType="com.yakx.web.domain.schedule.ClassroomReserve">
        select
        <include refid="column"></include>
        from  classroom_reserve cs
        left join classroom on cs.room_id = classroom.room_id
        left join course on cs.course_id = course.course_id
        left join teacher on cs.teacher_id = teacher.teacher_id
        left join clsases on cs.class_id = clsases.class_id
        where 1=1
        <if test="ew.sqlSegment !=''">
            and ${ew.sqlSegment}
        </if>
    </select>

    <select id="getPageList" resultType="com.yakx.web.domain.schedule.ClassroomReserve">
        select
        <include refid="column"></include>
        from  classroom_reserve cs
        left join classroom on cs.room_id = classroom.room_id
        left join course on cs.course_id = course.course_id
        left join teacher on cs.teacher_id = teacher.teacher_id
        left join clsases on cs.class_id = clsases.class_id
        where 1=1
        <if test="ew.sqlSegment !=''">
            and ${ew.sqlSegment}
        </if>
    </select>

</mapper>
